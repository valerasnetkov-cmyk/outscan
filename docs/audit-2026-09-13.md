# Daily audit - 2026-09-13

## B1 fixture image security remediation

- Started from clean b1-harness-review at reviewed 18d0c690230ff1f8ceaa5bfb3d38934ea651a3de;
  fetch and ff-only pull confirmed synchronization with origin.
- Source evidence now separates fixed fixtureRevision, pipelineRevision from
  actual git rev-parse HEAD, and workflowEventSha from GITHUB_SHA. Bootstrap
  binding of the future reviewed commit is deferred; no SHA is invented.
- Smoke create intent is saved before Docker create and acknowledged only after
  full ID/inspect/ownership validation. Empty discovery after ambiguity remains
  INCOMPLETE, late owned containers require verification before removal, and
  pending intent independently blocks publication even with stale PASS statuses.
- Added offline revision/lifecycle regressions and one b1_image unittest step to
  regular PR CI. Corrected token semantics: checkout uses github.token;
  persist-credentials=false avoids retaining it, while GHCR_TOKEN env is scoped
  to publication. BuildKit NOT APPROVED / NOT PINNED FOR EXECUTION.
- Local offline suites: b1_image 32/32 PASS; harness 31/31 PASS; Ubuntu policy
  32/32 PASS. Diff-check and source line-limit checks PASS. The first pnpm verify
  stopped on this audit's formatting; after correction, full pnpm verify PASS
  (format, lint, typecheck, application tests/builds and source line limit).
- No fixture workflow, builder, image build/pull/push, GHCR mutation or staging
  access occurred. These checks do not promote image/runtime evidence.

## B1 fixture image workflow — repository implementation

- Added a separate workflow_dispatch pipeline and modular Python helpers. The
  fixture source remains 929c40ecfe189be5f9aaf41f415bf7a376b7e475 with epoch 1789265917. The initial event-SHA field is corrected by the remediation above;
  accepted fixture source is never replaced by the pipeline checkout HEAD.
- Added strict image/registry gates, two independent pinned BuildKit builders,
  boundary-only smoke, systemd deadline and run-owned cleanup. Publication needs
  all gates, rejects any existing tag and verifies the registry response digest
  and pulled image identity. Manifest updates remain a separate reviewed commit.
- BuildKit digest is a required validated input without a default. No digest was
  fabricated. Checkout v4.2.2 and upload-artifact v4.6.2 commit pins were verified
  by read-only git ls-remote against the official action repositories.
- Existing ci.yml and fixture source are unchanged. No build/pull/push, container,
  package creation, workflow dispatch, staging access or runtime harness occurred.
- Validation: new image pipeline offline tests 24/24 PASS; existing B1 harness
  31/31 PASS; Ubuntu policy 32/32 PASS. pnpm verify PASS (format, lint, typecheck,
  1087 API tests with two skips, 23 capability tests, three web tests, builds and
  source line limit). git diff --check PASS. These are repository checks only.
- The first sandbox verify could not read node_modules (EPERM). A local retry
  outside the sandbox completed successfully; no packages or server settings
  were changed. No image/runtime PASS is claimed.
- Residual risks: external registry writer race, changing hosted runner/Buildx,
  prerequisite BuildKit review, dependency review and unexecuted Linux integration.
  Image NOT YET BUILT; isolation/egress NOT YET RUN; full Gate B1 IN PROGRESS.

## B1 Runtime Preparation acceptance

- B1 Runtime Preparation: PASS, configuration-only. Full Gate B1: IN PROGRESS.
- Repository was clean before this documentation-only update. Plan, changelog,
  deployment target and gate status now distinguish preparation from runtime evidence.
- No server commands, server changes, container creation/pull/start, scanners,
  deployment, permission changes or port publication were performed in this turn.

## Evidence provenance

- Prior direct read-only SSH through outscan-stage confirmed Ubuntu 24.04.5,
  kernel 6.8.0-139-generic, exact packages and holds, CLI/Compose/containerd
  versions, docker/containerd active and enabled, cgroup2fs, AppArmor enabled,
  outscan outside docker group, public TCP listeners limited to 22/80/443 and no
  TCP listeners on 2375/2376. docker0 was 172.17.0.1/16; IPv4 forwarding 1,
  IPv6 forwarding 0.
- Direct privileged checks were blocked by sudo authentication; no privileged
  rerun is claimed. Owner explicitly supplied administrative read-only outputs
  and attested no NOPASSWD, docker-group or socket-permission changes.
- Attached pasted-text.txt Docker JSON SHA-256:
  `6f3adeb92b09c39539dcc1b791925d184936a79737fb009c61e72e1456cc766a`.
  Its SystemTime is 2026-09-12T20:45:20.982468514Z (2026-09-13 locally).
  Raw daemon identifiers/topology were not copied into repository documentation.
- Owner UFW output: active, incoming deny/outgoing allow/routed deny; only
  22/80/443 allow rules for IPv4 and IPv6. Owner iptables output: empty
  DOCKER-USER chain exists. This does not prove egress restrictions or chain traversal.

## Evaluation

- Existing assessUbuntuHost policy evaluated the supplied Docker JSON locally
  with prior SSH OS/kernel/Compose observations and explicit pins
  24.04 / 29.8.0 / 5.4.0: PASS. Not a verify-ubuntu-host CLI run on Ubuntu.
- Additional assertions: systemd driver, zero containers/images, CLI 29.8.0,
  AppArmor security option and containerd commit matching prior 2.3.5 output: PASS.
- Docker JSON confirms Linux/Ubuntu and matching kernel, cgroup v2, builtin
  seccomp, MemoryLimit/SwapLimit/CpuCfsPeriod/CpuCfsQuota/PidsLimit true,
  Warnings null. These are daemon capability declarations, not pressure-test evidence.
- Exact held package baseline: docker-ce/docker-ce-cli
  5:29.8.0-1~~ubuntu.24.04~~noble; containerd.io 2.3.5-1~~ubuntu.24.04~~noble;
  docker-compose-plugin 5.4.0-1~~ubuntu.24.04~~noble.
- Existing policy test suite passed 32/32 in the preceding read-only check.
  Application lint/typecheck/build/DB/queue suites were not rerun for this
  documentation-only acceptance.
- Documentation git diff --check: PASS. Targeted pnpm exec prettier --check
  could not run because the prettier executable was unavailable; no dependency
  installation was performed and no formatting PASS is claimed.

## Remaining evidence

Target isolation/resource enforcement, IPv4/IPv6 private/link-local/metadata deny,
controlled DNS egress, cancellation/cleanup and reconciliation remain pending.
198.18.18.18 is not an approved scan target; DNS control-plane approval cannot
weaken target policy. Worker/secrets/Ops integration and remaining Guest/UI/WCAG
acceptance still block full B1; no public activation is authorized by this PASS.

## Repository-only B1 isolation/egress harness

- Implemented modular Python controller/config/transport/policy/topology/runtime,
  synthetic listener, main-process Node fixture programs, report schema and
  conservative state/reconciliation. No server connection or Docker operation
  was performed in this implementation turn.
- Added strict manifest/evidence schemas, explicit versioned deny registry and
  generated example manifest/plan/report. Image is null in the example; no real
  image digest was fabricated. Dockerfile reuses the existing immutable Node base,
  has no RUN dependency installation and was not built or published.
- Corrected lifecycle: started network-none anchor first; only after namespace
  policy/readback may a probe share its namespace. Positive controls cover IPv6
  and loopback. Link-local stays on-link. Controller-only SO_MARK controls cannot
  create a general ESTABLISHED bypass; probe capabilities remain empty.
- Independent systemd timer owns deadline cleanup. Controller must run inside a
  bounded systemd control group; watchdog verifies InvocationID before stopping
  it. Fault-run intentionally exits with owned resources for later recovery.
  Namespace intent/inode crash ambiguity fails closed rather than deleting by name.
- Added offline checks to CI and included Python in the 400-line source limit.
  Existing unrelated/uncommitted runtime-preparation documentation was preserved.
- Validation: 27 Python unit/policy/lifecycle/reconciliation tests PASS; existing
  Ubuntu policy suite 32/32 PASS; fixture Node syntax and targeted ESLint PASS.
  Initial registry extraction missed digit-bearing IPV4_MAPPED; fixed extraction
  and exact application-registry parity plus mapped-address tests now pass.
- Full application build/typecheck/DB/queue suites were not run: application code
  and dependencies are unchanged. No image build/load/pull/run/create/start,
  namespaces, veth, UFW/DOCKER-USER/sysctls, scanners or published ports were used.
- Remaining risks: unbuilt/unreviewed fixture identity; unexecuted Ubuntu kernel,
  namespace/systemd/iptables integration; additional deployment-range live fixture
  coverage; reboot recovery requires journal reconciliation. See
  [harness procedure](B1_ISOLATION_EGRESS_HARNESS.md) for commands and rollback.
- B1 Runtime Preparation remains PASS; B1 Isolation & Egress Evidence is
  NOT YET RUN; full Gate B1 remains IN PROGRESS.
- Final targeted Prettier/ESLint, Python compilation, 400-line check (including
  Python) and git diff --check: PASS; expected .gitignore CRLF warning only.
