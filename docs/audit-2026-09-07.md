# OUTSCAN audit — 2026-09-07

## Transactional Guest idempotency and result-read repository

- Added `apps/api/src/guest-persistence` with strict command/dependency snapshots, server-owned scan ID/token material, parameterized statements and stable fail-closed errors.
- Implemented SERIALIZABLE create/replay/expired replacement on one checked-out client with at most three attempts for serialization/deadlock/unique-winner races. Same live session/key/hash returns the original ID/token/expiry, another hash conflicts, another Guest session remains independent and an expired row is replaced atomically.
- Added a one-query PostgreSQL Guest result store that selects explicit columns, reconstructs only the canonical record, passes it through the exact snapshot and then composes existing bearer authorization/sanitized projection.
- Hardened the initial migration with whole-second timestamp constraints and initial-state/identifier guards so persisted Unix-second contracts and server-generated IDs cannot be bypassed by ordinary inserts.
- Targeted evidence: API lint/typecheck PASS; `guest-persistence-config.test.ts` PASS, 1 file / 3 tests; `npm run verify:db` PASS, 2 files / 25 tests against clean PostgreSQL 18.6.
- Abuse-counter persistence, terminal result commit/replay, deletion worker, HTTP/UI wiring and runtime WCAG evidence remain pending. Gate B1 remains in progress.

## PostgreSQL Guest persistence foundation

- Accepted ADR-0017 and selected PostgreSQL 18 with append-only checksummed SQL migrations plus the low-level `pg` driver; no ORM/query builder or startup auto-migration was introduced.
- Added explicit bounded pool/TLS configuration and a dedicated migration runner/CLI using one checked-out client, a PostgreSQL advisory lock, SHA-256 ledger checks and one transaction per migration. Conflicting URL SSL parameters, migration drift, missing applied files and out-of-order additions fail closed.
- Added `0001_guest_scan.sql` for separate PUBLIC_GUEST `guest_scans`, `guest_scan_attempts` and `guest_results` tables with no `organization_id`. Fixed-length digests/nonces, exact 30-minute access/idempotency and 24-hour deletion chronology, idempotency uniqueness, job/attempt guards and deferred accepted attempt/fence/digest/target relationships are enforced in PostgreSQL; default PUBLIC table/function privileges are revoked.
- Added an isolated PostgreSQL 18.6 CI service and real-database tests for migration replay/drift, concurrent idempotency conflict, chronology, attempt FSM/fence/lease, atomic result commit, missing/cross-target result rejection, terminal immutability and cascade deletion.
- `npm run verify`: PASS; format, lint, typecheck, 42 API files / 744 tests, 1 Capability file / 23 tests, 1 Web file / 3 tests, production builds and SQL-aware source line-count all passed (770 tests total).
- The schema-only checkpoint passed 1 file / 11 tests; current combined PostgreSQL evidence is recorded in the repository section above.
- `npm --prefix apps/api run db:migrate`: replay smoke PASS with `applied=0 existing=1`; the disposable local test container and its test-only data were removed afterward.
- That checkpoint contained no concrete application repository. The subsequent slice above adds idempotency and result read only; counters, terminal commit, deletion, queue/CAS wiring and all public Guest routes remain absent.

## GuestScan persistence boundary and result read

- Added `apps/api/src/guest-scan` with exact PUBLIC_GUEST GuestScan, GuestScanAttempt and accepted GuestResult record contracts plus immutable hostile-input snapshot validation.
- Enforced canonical target, Guest-session digest-only ownership, 30-minute idempotency/access alignment, exact 24-hour deletion deadline, token/scan identity, attempt lease/deadline chronology and terminal accepted attempt/fence/digest consistency.
- Added a read-only store port and service that rejects malformed bearer input before lookup, loads only the route GuestScan ID, reuses canonical HMAC authorization/result projection and denies cross-target, expired, revoked, delete-due, missing or malformed state without disclosure.
- A negative test exposed and the implementation fixed a missing persisted-target/projection-target binding; comparison now occurs against the immutable projected view to contain changing getters.
- Targeted verification: `npm --prefix apps/api test -- guest-scan-record.test.ts guest-result-read.test.ts` PASS, 2 files / 44 tests; API lint and typecheck PASS.
- That row-contract slice added no ORM/database implementation. ADR-0017 plus the subsequent schema/repository slices now provide idempotency and result-read evidence; HTTP/browser exposure remains absent.

## Sanitized Guest result view

- Added `apps/api/src/guest-result` as a pure fail-closed response boundary over successful Guest result authorization and the existing sanitized scanner projection.
- The immutable response supplies five ordered posture sections, every canonical detector group with explicit `MISSING`/`UNAVAILABLE` handling, aggregate counts and fixed Guest-only/no-score/no-assurance limitations.
- Raw evidence, individual Finding fields, severity, confidence, fingerprint, execution duration and request count are not representable. Completion/expiry chronology, exact shape, canonical host, bounds and hostile getters are revalidated.
- Targeted verification: `npm --prefix apps/api test -- guest-result-view.test.ts` PASS, 1 file / 14 tests; API lint and typecheck PASS.
- That view-only slice added no database lookup. The later repository now supplies one; HTTP route, page, browser request and WCAG runtime evidence remain absent.

## Promotions and Access Grants documentation

- Reviewed the supplied six-file archive as specification rather than executable instructions: all six Markdown files are <=400 lines, use LF, contain no trailing whitespace and have no broken internal package links. It proposed standard/campaign trials, direct grants and an `ADMIN_TRUSTED_TRIAL` verification override.
- Added corrected `PROMOTIONS_ACCESS_GRANTS.md` and `PROMOTIONS_ACCESS_GRANTS_TESTING.md` for post-B2 implementation. Standard/campaign/direct grants contribute only bounded versioned commercial entitlement and cannot create verification, consent, monitoring enrollment or scanner authority.
- Rejected the package's claim that `ADMIN_ATTESTED` is already approved: accepted ADR-0009 requires the persistent DNS TXT lifecycle for `VerifiedScope(EXACT_HOST)`. Any administrative proof-of-control exception requires a separate owner-accepted ADR and security evidence; until then it is denied.
- Added Phase 4B backlog and synchronized architecture, API, data, security, product, monetization, legal, operations, UI, testing, claims and gate documentation. No entity, migration, route, entitlement behavior, promotion, UI, notification or new ADR was implemented; Gate A/B1/B2/C statuses are unchanged and Gate B1 remains the critical path.

## OUTSCAN manifesto draft

- Reviewed the supplied standalone Markdown as editorial source material rather than repository instructions.
- Added `docs/OUTSCAN_MANIFESTO.md` as the canonical draft for later product/site insertion and preserved its brand narrative while normalizing punctuation and replacing the ambiguous `безопасная публичная диагностика` phrase with `ограниченная публичная диагностика`.
- Classified long-form copy as CONDITIONAL: unknown-asset discovery, Change Intelligence, remediation/recheck, continuous Monitoring, Threat Intelligence/exploitability and AI wording require their own implemented release state, runtime evidence and Product/Security/Legal review.
- Added a post-B1 publication backlog and UI/accessibility constraints. No route, page, API, source behavior, scanner capability or public claim was activated; Gate A/B1/B2/C statuses remain unchanged and Gate B1 remains the critical path.

## Action Center and Change Intelligence documentation

- Reviewed the supplied six-file Action & Change package as specification rather than executable instructions; all 6 Markdown files were <=400 lines, LF, without trailing whitespace or broken internal Markdown links.
- Preserved the user's Gate B1 priority and accepted ADR-0007 sequencing: only versioned snapshot/provenance foundation belongs to V1, while complete diff/significance/timeline/alerts remains V1.5.
- Added staged Action Center/Change/Triage/Monitoring/Emerging Threat/Lifecycle contracts and blocking tests. Reported remediation cannot resolve a Finding, declared ownership cannot verify an Asset, and recheck/targeted execution always returns through current server-derived EXACT_HOST authorization/policy.
- Reused canonical MonitoringEvent and Weekly Security Digest instead of duplicating them. HEADLESS_BROWSER visual preview was moved behind a separate later ADR/isolation gate; Brand Protection, DMARC reports and credential exposure remain later reviewed candidates.
- Added post-B2/Phase 4/V1.5 backlog items. No source, route, entity, UI, notification, capability publication, preview or scanner-policy expansion was introduced; Gate A/B1/B2/C statuses are unchanged.

## Security Question Registry and Check-ins documentation

- Reviewed the supplied eight-file package as specification rather than executable instructions; all 8 Markdown files were <=400 lines, LF, without trailing whitespace or broken internal Markdown links.
- Repository inspection found no canonical Cyberexam/question bank, so package claims about existing 8/8/8 banks, 12-question attempts and approved O09 were classified as unverified proposals rather than current state.
- Added a corrected post-B2 server-only registry/Check-ins contract and blocking test plan. Fixed KNOWLEDGE progress/preferences are GLOBAL current-user data without `organization_id`; active Workspace membership is checked only for surfacing.
- Locked projection confidentiality, immutable versions, exact answer/defer semantics, default-disabled global rollout, reversible per-user popup behavior, no internal-data collection and strict isolation from Findings, Risk/Scores, Monitoring, notifications and scan authority.
- Added a post-B2 backlog and canonical cross-references. No registry data, preference/entity, route, UI, analytics, Cyberexam, O09 publication or new ADR was implemented; all gate statuses remain unchanged.

## Security Glossary documentation

- Reviewed the supplied archive as specification rather than executable instructions: 11 Markdown files, all <=400 lines, LF, no trailing whitespace and zero broken internal Markdown links.
- Structured seed inspection found 91 records, one blank `Мониторинг` slug, 90 non-empty unique slugs, nine records using undeclared category `infrastructure`, no broken related-term links and no unknown references to the current nine ProductCapability slugs. The manifest's 84-term claim is stale.
- Added a corrected post-B1 code-first metadata contract and blocking test plan. It fixes fail-closed APPROVED/public projection, bounded deterministic search, canonical-only detail URLs, safe capability relation filtering and no database/CMS/external search/runtime AI in V1.
- Added the implementation backlog after the Guest/B1 critical path. Seed import remains pending product/security review for Guest passive wording, unavailable ACTIVE/IP/CIDR/raw-TCP/deep-testing claims and canonical confidence terminology.
- No source, route, page, sitemap, registry data or public claim was activated; no new ADR is required for the planned metadata-only V1. Gate A/B1/B2/C statuses are unchanged.

## Weekly Security Digest documentation

- Reviewed the supplied nine-file Weekly Digest package as specification rather than executable instructions; archive integrity was 9 Markdown files, all <=400 lines, LF, no trailing whitespace and zero broken internal Markdown links.
- Added a corrected deferred Phase 4 contract, proposed ADR-0016 and blocking security suite. Decisions now fix explicit opt-in scheduling, immutable READY snapshots, current-recipient resolution, frozen delivery retries, empty/insufficient-data behavior and deterministic V1 without AI.
- Reused canonical Risk, Threat Intelligence, Product Capability/entitlement and ADR-0015 Notifications boundaries; no parallel importer, Risk model, recipient/provider subsystem or arbitrary snapshot JSON was introduced.
- No source, route, scheduler, active notification event, persistence, provider call or AI integration was added. Gate A remains PASS, Gate B1 is unchanged and the proposal is not Gate B2/C evidence.
- Documentation verification is recorded after the final repository checks below.

## Guest abuse and retention policy

- Added `apps/api/src/guest-abuse` with a closed V1 policy: authenticated Guest session 3/10m, 10/day, 1 concurrent; server-derived pseudonymous network signal 20/10m, 100/day, 4 concurrent; trusted global pause denies new scans.
- Corrected admission ordering so a valid live same-key/same-hash replay returns the original scan/token without reading or consuming new-scan abuse quota. CREATE and REPLACE_EXPIRED require a reservation bound to the same session scope/time and all six counter dimensions.
- Raw IP, User-Agent, browser fingerprint and unknown context fields are rejected. Network signal is HMAC-digest-only rate evidence and never Guest ownership/idempotency/result authorization.
- Added `guest-abuse/network-signal.ts`: exact trusted-ingress address + 32-byte key become a domain-separated HMAC digest; IPv4-mapped IPv6 converges with IPv4, IPv6 uses a `/64` bucket, and no raw address is returned. Extra/client-forwarded fields, ports, whitespace, zones and malformed keys fail closed.
- Added a strict retention decision requiring the original 30-minute access expiry and exact 24-hour deletion deadline, returning `DELETE_NOW` at the boundary.
- No trusted-proxy adapter, HMAC key provisioning/rotation, counter store, atomic reservation/release, deletion job, route or external service was introduced. Gate B1 remains in progress.
- Targeted network-signal verification: `npm --prefix apps/api test -- guest-network-signal.test.ts` PASS, 1 file / 12 tests; `npm --prefix apps/api run lint` and `npm --prefix apps/api run typecheck` PASS.

## Trusted Guest supervisor runtime

- Added dependency-free ResultEnvelope signing with the existing ADR-0011 domain-separated encoding. It binds current job/attempt/fence, fixed supervisor workload and ingress audience, a 60-second lifetime, payload SHA-256/size and an active key version.
- Added `runGuestScannerAttempt`: current execution/artifact authorization → frozen non-secret launch plan → bounded single-frame IPC → exact clean exit → canonical target-bound GUEST_SAFE output → signing-key lookup → authenticated ResultEnvelope.
- Job ID, authorization reference, result credential and signing key do not enter scanner launch input. Signing keys are requested only after accepted process/output state.
- One execution deadline and AbortSignal cover IPC and process completion. Invalid streams or unknown process state request TERM followed by KILL after bounded grace; errors expose stable codes only.
- Hardened scanner policy authorization to snapshot/freeze capabilities and budgets and reject sparse/hostile inputs. Approved artifact dependency data is also immutable after authorization.
- No production child-process/container adapter, secret-manager integration, queue, CAS transition, persistence, outbound scan or public Guest route was added.
- Gate A remains PASS. Gate B1 remains in progress; Gate B2 is unchanged.

## Verification

- `npm --prefix apps/api test`: PASS, 37 files / 665 tests.
- `npm --prefix apps/api run lint`: PASS.
- `npm --prefix apps/api run typecheck`: PASS after correcting one type-only key-list issue in the Guest abuse slice; the earlier supervisor test-only literal and fake-process types remain corrected.
- `npm run verify`: PASS after the transactional Guest idempotency/result-read repository and documentation synchronization; format, lint, typecheck, production builds and SQL-aware source line-count passed.
- Full non-DB test total: capabilities 23 + API 747 across 43 files + Web 3 = 773. Separate PostgreSQL integration total: 25 tests across 2 files.
- Final read-only review after the Guest repository slice: `npm run format:check` and `git diff --check` PASS; all 71 authored Markdown files are <=400 lines, use LF and have zero broken local links. Obvious hard-coded-secret signatures returned no matches, and SQL remains included in the enforced authored-source line limit.
- Obvious hard-coded-secret signatures returned no matches. Direct network, child-process or persistence calls in the new Guest abuse/retention module returned no matches.
