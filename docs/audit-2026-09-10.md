# Audit — 2026-09-10

## Fixed scanner process and launch lifecycle

- Added `apps/api/src/supervisor/process-launcher.ts` without another dependency. It binds one exact approved artifact identity to an absolute executable/cwd and fixed bounded arguments, disables shell and environment inheritance, and sends only a bounded canonical scanner-input document to stdin.
- Exact plan shape, canonical hostname, current scanner policy and artifact/profile identity are revalidated before spawn. Spawn plus stdin initialization is capped at five seconds; stdout remains the bounded result IPC channel, stderr is ignored and the handle exposes TERM/KILL.
- Closed the supervisor deadline gap: one deadline and AbortSignal now cover launcher startup, IPC and exit. Cancellation of a pending launch returns promptly; if a valid handle resolves afterward it is killed without entering IPC or signing.
- Split generic process lifecycle handling into `process-lifecycle.ts` to retain the repository's 400-line authored-source limit; execution authorization and result semantics remain in `run-guest.ts`.

## Guest worker process entrypoint

- Added a dedicated API worker entrypoint that composes the existing PostgreSQL/BullMQ runtime, mounted approval and result-key providers, durable telemetry and fixed scanner process adapter. Its configuration permits only bounded absolute paths and a bounded JSON string array of deployment-owned scanner arguments.
- Startup requires the current approved GUEST_SAFE artifact and a fresh 256-bit signing key whose version and bytes match the immutable startup verification keyring. Missing/revoked/malformed approval, unsafe configuration and incomplete or conflicting key rotation deny without exposing provider details.
- The process emits only bounded local ready/telemetry status and stable failure text. It adds no HTTP route, outbound test scan, scanner capability or production deployment claim.

## Minimal first-party GUEST_SAFE scanner

- Added `apps/api/src/guest-scanner` and `guest-scanner-cli.ts` over the existing runtime DNS, full-set destination classifier, pinned HTTPS/same-host redirect flow and scanner IPC encoder. No dependency or public route was added.
- Scanner input independently requires the exact current GUEST_SAFE policy and DNS/TLS/HTTP capability subset. One counter bounds both A/AAAA queries and HTTPS dispatches; built-in or configured forbidden resolution cannot reach transport, inner retries are disabled and raw addresses/headers/body never enter output.
- Added an exact versioned runtime configuration joining DNS settings with a copied list of up to 128 deployment-specific internal CIDRs. Malformed, sparse, duplicate and hostile values fail closed; the same deny-list is used for preflight, retry and redirect resolution.
- Current evidence is deliberately partial: resolution/IPv6 and TLS reachability are observed; HSTS, CSP/frame protection, nosniff, referrer and permissions checks accept only a conservative useful subset. Positive HSTS requires non-zero `max-age`, CSP requires an enforcing directive, duplicate/ambiguous headers and permissive referrer/permissions values stay ATTENTION. This follows RFC 6797 and the W3C CSP, Referrer Policy and Permissions Policy processing models without claiming a complete browser parser.
- RDAP, CT, mail and public-infrastructure coverage is marked unavailable. The scanner has no platform credential, while container/egress isolation remains an external blocker.

## Bounded security.txt observation

- Added a second pinned HTTPS flow for the exact same-host `/.well-known/security.txt` path under the existing GUEST_SAFE HTTP capabilities and shared request counter. Redirects are still limited to the canonical host; cross-host redirects are observed but not followed and cannot pass.
- The dependency-free validator accepts only a conservative unsigned RFC 9116 subset: one `text/plain; charset=utf-8` response, fatal UTF-8, at most 32 KiB/1,000 lines/2,048 bytes per line, at least one supported Contact URI, exactly one future UTC Expires value and a matching Canonical URI when the field is present. BOM, control bytes, malformed or stale fields and ambiguous media types remain ATTENTION.
- Only the stable `SECURITY_TXT` outcome and fixed machine finding fields enter the scanner envelope. Response bodies, contacts, URIs, headers and socket addresses are not projected; presence or absence never grants scan authorization.

## Detached Guest result HTTP boundary

- Added `apps/api/src/guest-http` as an unregistered Fastify plugin over the existing authorized Guest result read port. It accepts only GET on `/v1/public/scans/:scanId`, disables Fastify's implicit HEAD route and rejects every query before reader invocation.
- Success emits only the sanitized view with fixed `no-store`/`no-referrer`. Malformed route/query maps to 400, all token/resource/persistence denials map to one enumeration-resistant 404, and unexpected clock/reader failure maps to a stable redacted 503.
- Six tests prove the mappings, privacy headers, failure containment and that the production `buildApp()` still returns 404 for the route. No Guest route was registered or exposed; deployment controls remain authoritative.

## Detached Guest session bootstrap boundary

- Added an unregistered Fastify plugin for `POST /v1/public/guest-session`. It accepts no query/body, requires one exact configured HTTPS Origin, handles a single raw Cookie header and passes only that value to an injected server-owned bootstrap decision.
- A reused session returns `204` without refreshing its lifetime. Issuance returns only a strictly validated `__Host-outscan_guest_session` Set-Cookie header with the fixed Secure/HttpOnly/SameSite/Path/Max-Age contract; the body is empty and all outcomes use no-store/no-referrer/nosniff.
- Unknown/extra service fields, header injection, unsafe cookie attributes, provider failure and malformed origin configuration fail closed. Production `buildApp()` still returns 404.
- Added that concrete dependency-injected service: every call freshly validates and copies a maximum-three active/retained 32-byte keyring, authenticates before scope-revocation lookup, reuses without Set-Cookie refresh and issues a new active-key scope for missing/invalid/revoked credentials. Removed retained keys and provider failures take effect immediately; production key provisioning remains external.

## Detached Guest scan creation HTTP boundary

- Added an unregistered Fastify plugin for `POST /v1/public/scans` over the existing creation service. It requires the exact deployment-configured HTTPS Origin, rejects every query, bounds the body to 1 KiB and permits only JSON plus single raw Cookie, Idempotency-Key and forwarding headers.
- The adapter passes only the parsed body, raw authentication/idempotency inputs, socket peer and forwarding value into the trusted service. It reconstructs fixed no-store/no-referrer/nosniff responses and accepts only the documented `200/202/400/401/409/429/503` status/code combinations.
- Service headers and bodies are revalidated through an exact allowlist. Unknown fields/codes, mismatched status, malformed success/token/time data, injected headers, invalid `Retry-After`, parser errors and thrown dependencies collapse to bounded public errors without internal detail.
- Twenty-five focused cases pass, including new/replay success, every public error class, rate-limit responses with and without bounded `Retry-After`, cross-origin/query/media/body denial, duplicate sensitive headers, hostile decisions and continued 404 from production `buildApp()`.

## Durable Guest-session revocation

- Added `0007_guest_session_revocations.sql`: one PUBLIC_GUEST operational row per 32-byte scope digest, exact second-aligned 24-hour lifetime, expiry index and revoked PUBLIC privileges. Raw cookie/session, target, network and tenant identity are not representable.
- Added an idempotent PostgreSQL revoke/read adapter plus bounded `SKIP LOCKED` expiry pruning. Concurrent revoke converges without extending an active window; exact-expiry replacement starts a new fixed window.
- Guest scan creation now awaits the same provider after MAC authentication. Confirmed revoke maps to the bounded `401`, while provider exception/non-boolean state maps to redacted `503` before persistence or queue access. No public revoke endpoint or operator UI was added.

## Guest-session keyring and retention integration

- Added a strict fresh-read mounted Guest-session keyring provider for one active and at most two retained 32-byte canonical base64url MAC keys. Active version must exist; duplicate/unknown fields or versions, malformed/oversized JSON, missing files and unsafe paths fail closed.
- Extracted the existing regular-file reader into a shared trusted-file boundary. Supervisor behavior is preserved while Guest-session keys receive the same bounded fatal UTF-8, duplicate-key, symlink/lstat-open identity and private POSIX-permission checks; Windows ACL remains deployment evidence.
- Integrated expired revocation pruning into each bounded Guest retention batch. Revocation backlog participates in the same maximum-ten-batch budget; malformed/unavailable cleanup becomes `GUEST_RETENTION_UNAVAILABLE`, and no second scheduler was introduced.
- Added append-only migration `0008_guest_retention_revocations.sql` and the minimized `session_revocations_deleted` counter to immutable 30-day PLATFORM retention telemetry. No cookie, scope digest, target, tenant or scanner value is copied into the run record.

## Fresh-read Guest-session scan admission

- Replaced the creation service's long-lived Guest-session keyring Map dependency with the shared `GuestSessionKeyProvider`. The provider function is bound at composition, then called and strictly snapshotted for every scan request.
- Bootstrap and scan admission now apply the same exact active/retained version and 32-byte key invariants. Removing a retained key makes its old cookie invalid on the next admission before revocation lookup, PostgreSQL mutation or BullMQ enqueue.
- Missing, malformed, non-Map, throwing or otherwise ambiguous key state maps to `503 GUEST_SCAN_UNAVAILABLE`; no provider detail or key material crosses the service boundary. Confirmed cookie invalidity remains the documented 401.
- Targeted bootstrap/create/route evidence passed 55 tests. The PostgreSQL creation harness uses the same provider contract; no migration, public route, scanner behavior or external credential was added.

## Mounted-file read budget hardening

- Shared key/approval reader now allocates only the validated file size plus one sentinel byte, uses bounded positional reads, and rechecks size/mtime/ctime/permissions before parsing. This closes unbounded allocation during concurrent growth; it does not establish filesystem snapshot isolation or Windows ACL evidence.
- Added 12 deterministic unit cases for partial reads, growth, truncation, post-read metadata changes, error cleanup and invalid budgets. Targeted mounted-reader/Guest-keyring/supervisor-provider tests passed: 40 passed / 2 POSIX-only cases skipped on Windows.
- Current `npm run verify`: PASS (format, lint, typecheck, tests, builds, source line limits): registry 23 + API 1,006 across 64 files + web 3 = 1,032 passed / 2 skipped. Markdown/link checks passed for 74 files; obvious secret-signature search returned no matches and `git diff --check` passed. Database/Redis suites were not rerun because their code/contracts were unchanged in this slice; prior evidence is recorded below.

## Verification before this slice

- `npm run verify`: PASS. Format, lint, typecheck, unit tests, production builds and authored source line limits passed; Product Capability Registry 23 + API 994 passed / 2 Windows-inapplicable POSIX cases skipped across 63 files + Web 3 = 1,020 passed / 2 skipped.
- Repository hygiene after the slice: 74 Markdown files, LF, all at or below 400 lines, no invalid trailing whitespace, broken local Markdown links or obvious secret signatures, and `git diff --check` PASS. Existing two-space CommonMark hard breaks in older documents were preserved.
- Nine local process-adapter tests plus one supervisor pending-launch/late-handle negative passed. Tests use only the installed Node executable and perform no DNS query or outbound request.
- `npm run verify:db` passed against an ephemeral loopback PostgreSQL 18.6 container: 12 files / 81 tests, including the creation harness after its key-provider refactor, migration `0008`, exact replay and integrated revocation-cleanup telemetry. The container was then stopped and removed. Queue transport was unchanged, so `npm run verify:queue` was not rerun; its latest recorded evidence remains 2 Redis tests.

## Guest bootstrap parser error containment

- Added route-local 1 KiB body ceiling and redacted framework error mapping: parser 400/413/415 become `INVALID_GUEST_SESSION_REQUEST`; other failures become `GUEST_SESSION_UNAVAILABLE`. Both paths carry no-store/no-referrer/nosniff.
- Four new negative cases exercise malformed/empty/oversized JSON and unsupported media types, proving no cookie issuance and no bootstrap call. The route remains detached; database, queue, scanner scope and deployment are unchanged.
- Verification: `npm run verify` PASS, including format/lint/typecheck/build/source limits and 1,036 tests (registry 23, API 1,010, web 3), with 2 POSIX-only skips. All 74 Markdown files passed LF/line-count/local-link checks; obvious secret signatures were absent and `git diff --check` passed. PostgreSQL/Redis suites were not rerun for this HTTP-only slice.

## Guest result HTTP projection

- Added `guest-http/result-projection.ts`: reconstructs the canonical view using bounded public posture/coverage inputs, enforces exact deep equality with the service decision and returns only rebuilt objects. Unknown top-level/nested fields, serialization hooks, extra headers, cross-scan IDs, inconsistent coverage/counts and invalid completion/access times fail closed.
- The route now rejects non-bigint/out-of-range clock values before reader access and contains malformed/unknown reader decisions behind the existing redacted 503 response. The public app remains unchanged.
- Targeted result-route suite: 27 PASS, including 21 new hostile-result/decision/clock cases. No database, queue or scanner policy change.
- Full `npm run verify`: PASS, including format/lint/typecheck/build/source limits, registry 23 + API 1,031 + web 3 = 1,057 passed / 2 POSIX-only skips. All 74 Markdown files passed LF/line-count/local-link checks; obvious secret-signature search had no matches and `git diff --check` passed. DB/Redis suites were not rerun for this HTTP-only slice.

## Guest bootstrap-to-scan HTTP composition

- Added `apps/api/test/guest-http-composition.test.ts`, composing both detached HTTP plugins with the actual bootstrap and scan creation services and shared key/revocation providers. Five scenarios passed: cookie issuance/admission/no-refresh reuse, absent/tampered cookies, key removal without restart, revocation/provider ambiguity and enqueue-failure/persisted-replay recovery.
- Persistence and queue are scripted test ports; this does not establish transaction/idempotency correctness, Redis reliability or deployment evidence. No public registration, production source change, database migration or outbound scan was introduced.
- First full verification stopped at two unused test parameters; both were corrected. Repeated `npm run verify` PASS: format/lint/typecheck/build/source limits and 1,062 tests (registry 23 + API 1,036 in 65 files + web 3), with 2 POSIX-only skips. All 74 Markdown files passed LF/line-count/local-link checks, obvious secret signatures were absent and `git diff --check` passed. Database/Redis suites were not rerun in this test-only slice.

## Ubuntu/Compose target acceptance

- Owner selected Ubuntu + Docker Compose for the current OUTSCAN version and later migration of scanner execution workers to Kubernetes. Recorded the accepted direction, B1 delivery sequence and unchanged security/job contracts in `DEPLOYMENT_TARGET.md`; synchronized README, architecture, operations, ADR-0003 clarification and plan.
- Updated the changelog by consolidating the adjacent Guest HTTP summaries without dropping their evidence, retaining its 400-line ceiling. No source, Compose deployment, host configuration, credentials, live target test or Gate status changed. Ubuntu/runtime versions and the launch/isolation mechanism still require implementation/configuration and review.
- Documentation verification: `npm run format:check` and `git diff --check` PASS; 75 Markdown files passed LF/400-line/local-link checks. Source tests/builds were not rerun for this documentation-only decision; their latest evidence is recorded above.

## Ubuntu/Compose API baseline

- Added default-deny `.dockerignore`, digest-pinned multi-stage `deploy/compose/api.Dockerfile`, opt-in no-network Compose profile, runtime smoke and a separate Ubuntu 24.04 CI job. Runtime carries built API/capabilities plus frozen production dependencies only; no source/public/env/Docker socket mounts, scanner or database is started.
- Legacy pnpm deploy could not package offline without registry metadata; replaced it with a separate `pnpm install --prod --frozen-lockfile --ignore-scripts --offline` layer (85 packages reused, zero downloaded). Final image build passed with Node 24.21.0; no dependency/lockfile change was required.
- Docker Desktop Linux Engine 29.7.2 / Compose 5.5.0 local runtime evidence: Compose config/build/up-healthcheck and explicit `API_BASELINE_SMOKE_PASS`. Inspected UID 1000:1000, readonly root, network none, no port bindings/mounts, 256 MiB memory, 0.5 CPU and 64 PIDs. Smoke also checked zero effective capabilities, no-new-privileges, absent runtime source/public/env/socket paths, health, empty capabilities and Guest session/create/result 404.
- The temporary `outscan-baseline-check-api-baseline-1` container was stopped and removed; its reproducible local image/build cache remains. Docker Desktop was started for verification. This is local Linux-container evidence, not target Ubuntu server or remote CI evidence, image vulnerability approval, scanner isolation/egress or public deployment.
- After correcting smoke-script lint/format findings, final image build/up/explicit smoke passed again and the container was removed. `npm run verify` PASS (format/lint/typecheck/build/source limits; 1,062 passed / 2 POSIX-only skips); explicit smoke-script ESLint PASS. All 75 Markdown files passed LF/400-line/local-link checks; obvious secret signatures were absent and `git diff --check` passed. PostgreSQL/Redis suites were not rerun because this slice changed no data/queue behavior; remote CI was not invoked.

## Gate status after API baseline

- Gate A remains PASS.
- Gate B1 remains IN PROGRESS. Container/OS isolation, enforced egress policy, deployment binding, managed secret/approval providers, worker/retention deployment, approved alert export, owned-target live socket/TLS, public route registration/UI and runtime WCAG evidence remain blocking. The local worker CLI and detached HTTP plugins are composition evidence only.
- No Guest HTTP route, live outbound test or additional scanner capability was exposed.
