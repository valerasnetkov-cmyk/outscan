# Audit — 2026-09-11

## Scope

Continued Ubuntu/Compose B1 preparation from the [previous audit](audit-2026-09-10.md).
No public Guest route, outbound Internet scan, production launcher, credential or
host egress configuration was introduced.

## Offline scanner CLI container evidence

- Added `deploy/compose/scanner-smoke.mjs` and opt-in `scanner-check` Compose profile.
  A disposable non-root/read-only/no-network container starts the actual scanner
  CLI with empty child environment and bounded stdin/IPC/time/output handling.
- Two runtime cases use absent IPv4/IPv6 loopback resolvers and validate canonical
  output: two DNS calls, zero detected risks and failed/unknown coverage for every
  group. Three rejection cases cover malformed JSON, a URL target and input above
  16 KiB; all return exit 1, the fixed stderr message and no valid result frame.
- The real Linux-container build and one-shot run passed with
  `SCANNER_OFFLINE_SMOKE_PASS`. The run container was automatically removed via
  `--rm`; the local image/cache remains. No external target was contacted.
- Added the offline check to the existing Ubuntu CI job and included both smoke
  scripts in CI lint. Remote CI has not been run. The test reuses the baseline image
  without starting API/supervisor and is not a minimal production scanner image,
  privileged launch broker, live egress or target Ubuntu server evidence.

## Verification

- `npm run verify`: PASS — format, lint, typecheck, builds, authored-source limits;
  registry 23 + API 1,036 across 65 files + web 3 = 1,062 passed / 2 POSIX-only skips.
- Both Compose smoke scripts passed explicit ESLint. Compose config/build and the
  five offline scanner scenarios passed separately from the unit-test count.
- 76 Markdown files passed LF/400-line/local-link checks; obvious secret-signature
  search found no matches and `git diff --check` passed. DB/Redis suites were not
  rerun because no persistence/queue behavior changed in this slice.

## Host-side container lifecycle evidence

- Added `scripts/verify-scanner-lifecycle.mjs` to verify the existing offline Compose
  boundary with fixed failure and SIGTERM-ignoring fixtures. Commands are shell-free
  and bounded; cleanup validates random name/run label/Compose project, removes the
  exact inspected container ID and verifies absence. Daemon failure is not absence.
- Local build and `SCANNER_CONTAINER_LIFECYCLE_PASS`: exit 23 and TERM-to-KILL exit
  137 after a one-second grace were confirmed; both run containers were removed and
  the verification project had no remaining containers. No scanner job, outbound
  probe, secret, host firewall mutation or production launcher was introduced.
- Added the host harness and scoped always-run cleanup to CI. CI execution and
  target-Ubuntu evidence are pending; production supervisor lifecycle integration
  and crash reconciliation are still required. This is explicitly a test harness.
- Full `npm run verify`: PASS (format/lint/typecheck/build/source limits; 1,062
  passed / 2 POSIX-only skips). Explicit lifecycle-script ESLint passed. All 76
  Markdown files passed LF/400-line/local-link checks, obvious secret signatures
  were absent and `git diff --check` passed. DB/Redis suites were not rerun for
  this infrastructure-test slice; the two lifecycle cases are separate evidence.

## Detached offline container launcher

- Added an actual host-side `ScannerProcessLauncher` using bounded shell-free Docker
  commands, fixed local endpoint, exact image-ID/artifact binding, non-overridable
  offline container flags and run-label/name/ID cleanup. Scanner input encoding was
  extracted from the existing fixed process launcher and reused without widening
  policy or credential access. The worker CLI remains on its existing adapter.
- Added unit negatives and `scripts/verify-offline-launcher.mjs`. A mocked execFile
  promisify contract and a runtime fixture missing required scanner capabilities
  were corrected. Real host → create/inspect → attach/stdin → canonical IPC → wait/
  cleanup passed with `OFFLINE_CONTAINER_LAUNCHER_PASS`; no run containers remained.
- Synthetic approval metadata/local image ID and no-network DNS failure are test
  evidence only. Host Docker privilege/broker review, image promotion, live egress,
  worker binding and reconciliation after daemon/host/ambiguous-create failures
  remain required before production. CI wiring is present but was not run remotely.

## Current gate status

Gate A remains PASS; Gate B1 remains IN PROGRESS. Production scanner launch and
isolation/egress, provisioning, worker/retention deployment, approved Ops export,
owned-target live TLS, public UI/route registration and WCAG evidence remain pending.
